<!DOCTYPE html>
<html lang="en">

    <html>

        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <link rel="stylesheet" href="/styleRecipeDetails.css">
            <title>Foodfy - Receitas</title>
        </head>

        <body>
            <header>
                <a id="logo" href="/"><img src="/assets/logo.png" alt="logo Foodfy"></a>

                <a id="about" href="/about">Sobre</a>
                <a id="recipes" href="/recipes">Receitas</a>
                <a id="chefs" href="/chefs">Chefs</a>

            </header>

            <div id="intro">
                <img id="image" src="{{recipe.image}}" alt="foto de {{recipe.title}}">
                <h1 id="title">{{recipe.title}}</h1>
                <p id="author">por
                    {{recipe.name}}</p>
            </div>

            <div id="ingredients">
                <h2>Ingredientes
                    <button id="buttonIngredients">ESCONDER</button>
                </h2>
                <ul>
                    {% for ingredient in recipe.ingredients %}
                        <li>{{ingredient}};</li>
                    {% endfor %}
                </ul>
            </div>

            <div id="prepareMethod">
                <h2>Modo de preparo
                    <button id="buttonMethod">MOSTRAR</button>
                </h2>
                <ul class="hide">
                    {% for prepareMethod in recipe.preparation %}
                        <li>{{prepareMethod}}</li>
                    {% endfor %}
                </ul>
            </div>

            <div id="aditionalInfo">
                <h2>Informações Adicionais
                    <button id="buttonAditional">ESCONDER</button>
                </h2>
                <p>{{recipe.information}}</p>
            </div>

            <footer>
                <p>Todos direitos reservados, receitas Foodfy.</p>
            </footer>

            <script src="/scripts.js"></script>

        </body>

    </html>