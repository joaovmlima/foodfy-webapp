<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/css/adminStyles/styleAdminRecipeDetails.css">
        <title>Foodfy</title>
    </head>

    <body>
        <header>
            <a id="logo" href="/admin"><img src="/assets/logo-white.png" alt="logo Foodfy"></a>
            <a href="/admin/recipes">Receitas</a>
            <a href="/admin/chefs">Chefs</a>
        </header>

        <div class="recipeHeader">
            <h1>Receita:
                {{recipe.title}}
            </h1>
            <a href="/admin/recipes/{{recipe.id}}/edit">
                <button>Editar receita</button>
            </a>
        </div>

        <div id="intro">
            <img id="image" src="{{recipe.image}}" alt="foto de {{recipe.title}}">
            <h1 id="title">{{recipe.title}}</h1>
            <p id="author">por
                {{recipe.name}}
            </p>
        </div>

        <div id="ingredients">
            <h2>Ingredientes</h2>
            <ul>
                {% for ingredient in recipe.ingredients %}
                    <li>{{ingredient}};</li>
                {% endfor %}

            </ul>
        </div>

        <div id="prepareMethod">
            <h2>Modo de preparo</h2>
            <ul>
                {% for prepareMethod in recipe.preparation %}
                    <li>{{prepareMethod}}</li>
                {% endfor %}
            </ul>
        </div>

        <div id="aditionalInfo">
            <h2>Informações Adicionais</h2>
            <p>{{recipe.information}}</p>
        </div>

        <footer>
            <p>Todos direitos reservados, receitas Foodfy.</p>
        </footer>

    </body>